packages:
  - .
overrides:
  # next 16.2.9 pulls baseline-browser-mapping@2.10.37, published within pnpm's
  # default minimumReleaseAge window; pin the prior release to satisfy the policy.
  baseline-browser-mapping: 2.10.36
allowBuilds:
  esbuild: false
  msw: false
  sharp: false
  unrs-resolver: false
